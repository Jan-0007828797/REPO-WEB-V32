diff --git a/app/game/[gameId]/page.js b/app/game/[gameId]/page.js
index 1111111..2222222 100644
--- a/app/game/[gameId]/page.js
+++ b/app/game/[gameId]/page.js
@@ -1,6 +1,7 @@
 import React from "react";

 // ... existující kód

@@ -1450,6 +1450,38 @@ export default function GamePage() {

   // ===== AUDIT PHASE UI =====

+  const allAuditStarted =
+    gs?.players?.length &&
+    gs.players.every((p) => p?.auditStarted);
+
+  const FinalAuditBanner =
+    gs?.phase === "SETTLE" && me?.auditStarted ? (
+      <div
+        style={{
+          background: "#ff1f1f",
+          color: "#fff",
+          borderRadius: 14,
+          padding: "14px",
+          display: "flex",
+          alignItems: "center",
+          gap: 14,
+          marginTop: 12,
+        }}
+      >
+        <div
+          style={{
+            width: 58,
+            height: 58,
+            borderRadius: 14,
+            background: "#b80000",
+            display: "flex",
+            alignItems: "center",
+            justifyContent: "center",
+            fontSize: 40,
+          }}
+        >
+          📄
+        </div>
+        <div style={{ fontWeight: 800 }}>
+          {allAuditStarted
+            ? "Finální audit – detailní vyúčtování"
+            : "Finální audit – čeká se na audit ostatních hráčů"}
+        </div>
+      </div>
+    ) : null;

   return (
     <div className="gameScreen">
@@ -2100,6 +2132,9 @@ export default function GamePage() {

       {/* AUDIT UI */}

+      {FinalAuditBanner}
+
+
       {gs?.phase === "SETTLE" && !me?.auditStarted && (
         <button
           className="primaryBtn big full"
